API_KEY=pick-a-long-random-string

# Web Push (desktop PWA) -- see README.md "Desktop app" section.
VAPID_PRIVATE_KEY_PEM=
VAPID_PUBLIC_KEY=
VAPID_CLAIM_EMAIL=mailto:admin@ndgroup.example

# GitHub Pages origin the web build is served from (host only, not the full
# path) -- CORS checks the Origin header, which never includes a path.
CORS_ORIGINS=https://ndzicyril501-max.github.io
